#include <iostream>

using namespace std;

int Eval(string expr) {
  
}

int main() {
  int n;
  cin >> n;
  for (int i = 0; i < n; ++i) {
    string line;
    getline(cin, line);
    cout << Eva(line) << endl;
  }
  return 0;
}
